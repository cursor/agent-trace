{
  "version": "1.0.0",
  "scripts": {
    "build": "bun run index.ts",
    "dev": "bun run index.ts --dev",
    "gen-schema": "bun run scripts/gen-schema-json.ts"
  },
  "dependencies": {
    "marked": "^15.0.0",
    "marked-shiki": "^1.2.0",
    "shiki": "^3.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
